export const CLI = () => (
  <Tooltip
    headline="CLI"
    tip="Command Line Interface — a text-based way to interact with software by typing commands into a terminal window."
  >
    CLI
  </Tooltip>
);
