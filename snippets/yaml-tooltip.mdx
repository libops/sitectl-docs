export const YAML = () => (
  <Tooltip
    headline="YAML"
    tip="YAML is a human-readable text format widely used for configuration files. sitectl stores context settings in a YAML file at ~/.sitectl/config.yaml."
  >
    YAML
  </Tooltip>
);
