{/* Auto-generated from source. Run `make docs-snippets` to update. */}

Create a new project

```bash
sitectl libops create project
```

| Flag | Default | Description |
|------|---------|-------------|
| `--create-branch-sites` | `false` | Auto-create sites for new branches |
| `--disk-size-gb` | `20` | Disk size in GB |
| `--machine-type` | `e2-standard-2` | GCP machine type |
| `--name` |   | Project name (required) |
| `--organization-id` |   | Organization ID (required) |
| `--region` | `us-central1` | GCP region |
| `--zone` | `us-central1-f` | GCP zone |
