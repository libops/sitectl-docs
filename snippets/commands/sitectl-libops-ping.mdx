{/* Auto-generated from source. Run `make docs-snippets` to update. */}

Ping a site URL to wake it

```bash
sitectl libops ping <site-id-or-url>
```

| Flag | Default | Description |
|------|---------|-------------|
| `--timeout` | `30s` | HTTP timeout |
