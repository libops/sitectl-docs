{/* Auto-generated from source. Run `make docs-snippets` to update. */}

List secrets. Optionally filter by `--organization-id`, `--project-id`, or `--site-id`. If no filter is specified, lists all secrets.

```bash
sitectl libops list secrets
```

| Flag | Default | Description |
|------|---------|-------------|
| `--no-cache` | `false` | Disable cache and fetch fresh data |
| `--organization-id` |   | Filter by organization ID |
| `--project-id` |   | Filter by project ID |
| `--site-id` |   | Filter by site ID |
