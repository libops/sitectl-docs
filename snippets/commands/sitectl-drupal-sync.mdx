{/* Auto-generated from source. Run `make docs-snippets` to update. */}

Copy Drupal artifacts from one sitectl context to another.

Available artifacts: database, config (Drupal config/sync directory)

```bash
sitectl drupal sync <command>
```
