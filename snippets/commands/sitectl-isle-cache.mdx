{/* Legacy snapshot from sitectl-isle v0.19.0. ISLE is not in the sitectl v1 generator set. */}

Cache warmer commands to speed up your ISLE site page load times

```bash
sitectl isle cache <command>
```
