{/* Auto-generated from source. Run `make docs-snippets` to update. */}

Edit a project

```bash
sitectl libops edit project <project-id>
```

| Flag | Default | Description |
|------|---------|-------------|
| `--create-branch-sites` | `false` | Auto-create sites for new branches |
| `--disk-size-gb` | `0` | Disk size in GB |
| `--machine-type` |   | GCP machine type |
| `--name` |   | Project name |
