{/* Auto-generated from source. Run `make docs-snippets` to update. */}

Cron specs define scheduled jobs for a sitectl context. Each spec records which context
to run on, what schedule to use, which job components to run, and where to store output.

Use render-systemd to turn a spec into systemd units you can install on the host.

```bash
sitectl cron <command>
```
