{/* Auto-generated from source. Run `make docs-snippets` to update. */}

List components registered by the active context's plugin, including allowed dispositions and component-specific set flags.

```bash
sitectl component list [component]
```
