{/* Legacy snapshot from sitectl-isle v0.19.0. ISLE is not in the sitectl v1 generator set. */}

Copy ISLE artifacts from one sitectl context to another.

Available artifacts: fcrepo (Fedora Commons database)

```bash
sitectl isle sync <command>
```
