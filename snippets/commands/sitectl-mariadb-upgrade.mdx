{/* Auto-generated from source. Run `make docs-snippets` to update. */}

Upgrade MariaDB system tables when required

```bash
sitectl mariadb upgrade
```

| Flag | Default | Description |
|------|---------|-------------|
| `--service` | `mariadb` | MariaDB compose service name |
