{/* Auto-generated from source. Run `make docs-snippets` to update. */}

Reply to a task that needs input

```bash
sitectl libops task respond <task-id> <message>
```

| Flag | Default | Description |
|------|---------|-------------|
| `--no-wait` | `false` | Return after queueing the reply |
| `--organization-id` |   | Organization ID |
| `--poll-interval` | `3s` | Task polling interval while attached |
