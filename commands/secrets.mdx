---
title: secrets
description: Inspect, generate, and rotate Compose-declared filesystem or Vault-backed secrets.
---

import SecretsCmd from "/snippets/commands/sitectl-secrets.mdx";
import SecretsListCmd from "/snippets/commands/sitectl-secrets-list.mdx";
import SecretsGenerateCmd from "/snippets/commands/sitectl-secrets-generate.mdx";
import SecretsRotateCmd from "/snippets/commands/sitectl-secrets-rotate.mdx";

List the active site's declared secret references before generating missing
values or rotating one value:

```bash
sitectl secrets list
sitectl secrets generate
sitectl secrets rotate APP_DB_PASSWORD
```

Filesystem-backed values live under the site's configured secrets directory.
Vault-backed values are piped to the Vault CLI over stdin and are not copied
into the project directory. Rotation changes stored material; restart or deploy
the consumers that need to reopen their secret files, then run healthcheck and
application verification.

## Reference

<SecretsCmd />

### `secrets list`

<SecretsListCmd />

### `secrets generate`

<SecretsGenerateCmd />

### `secrets rotate`

<SecretsRotateCmd />
